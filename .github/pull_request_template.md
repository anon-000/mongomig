## What and why

<!-- What does this change, and which issue does it address? -->

## Checklist

- [ ] `make check` passes (ruff, mypy --strict, pytest)
- [ ] Tests added or updated (integration tests for anything touching MongoDB)
- [ ] Docs (`docs/`) and `CHANGELOG.md` (*Unreleased*) updated for user-visible changes
- [ ] No automatic data deletion introduced; credentials never printed
